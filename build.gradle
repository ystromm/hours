plugins {
  id "com.moowork.node" version "0.11"
}

node {
    download = true
    version = '4.2.3'
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.9'
}

task install(type: NpmTask) {
    args = ['install']
}

task test(type: NpmTask, dependsOn: 'install') {
    args = ['test']
}
